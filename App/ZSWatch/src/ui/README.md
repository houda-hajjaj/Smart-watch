# View portable - arborescence type ui

Cette arborescence suit le template `ui/` :
- `components/` : moteurs internes de vue
- `screens/` : fichiers ecrans
- `images/` : assets
- `ui*.c/h` : facade type SquareLine

Les fichiers d'origine sont conserves, seule l'arborescence est reorganisee.
